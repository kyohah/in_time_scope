name: Deploy mdBook Documentation

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install mdBook and i18n-helpers
        run: |
          sudo apt-get update
          sudo apt-get install -y gettext
          cargo install mdbook mdbook-i18n-helpers

      - name: Generate POT file
        working-directory: docs
        run: |
          MDBOOK_OUTPUT='{"xgettext": {}}' mdbook build -d po

      - name: Build English documentation
        working-directory: docs
        run: mdbook build -d book/en

      - name: Build Japanese documentation
        working-directory: docs
        run: MDBOOK_BOOK__LANGUAGE=ja mdbook build -d book/ja

      - name: Build Chinese documentation
        working-directory: docs
        run: MDBOOK_BOOK__LANGUAGE=zh mdbook build -d book/zh

      - name: Build German documentation
        working-directory: docs
        run: MDBOOK_BOOK__LANGUAGE=de mdbook build -d book/de

      - name: Build French documentation
        working-directory: docs
        run: MDBOOK_BOOK__LANGUAGE=fr mdbook build -d book/fr

      - name: Create language index
        run: |
          cat > docs/book/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <meta http-equiv="refresh" content="0; url=en/">
            <title>InTimeScope Documentation</title>
            <style>
              body { font-family: system-ui; max-width: 600px; margin: 50px auto; padding: 20px; }
              h1 { color: #333; }
              ul { list-style: none; padding: 0; }
              li { margin: 10px 0; }
              a { color: #0066cc; text-decoration: none; font-size: 18px; }
              a:hover { text-decoration: underline; }
            </style>
          </head>
          <body>
            <h1>InTimeScope Documentation</h1>
            <p>Select your language:</p>
            <ul>
              <li><a href="en/">English</a></li>
              <li><a href="ja/">日本語</a></li>
              <li><a href="zh/">中文</a></li>
              <li><a href="de/">Deutsch</a></li>
              <li><a href="fr/">Français</a></li>
            </ul>
            <p><small>Redirecting to English...</small></p>
          </body>
          </html>
          EOF

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
